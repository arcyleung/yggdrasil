# Issue tracker

- **System:** GitHub Issues
- **Repo:** `arcyleung/yggdrasil` (remote `origin`)
- **CLI:** `gh` (token needs `repo`; `read:org` optional)
- **External PRs as triage surface:** no (collaborator PRs are implementation, not intake)
- **Local fallback:** `.scratch/<feature>/` markdown if `gh` unavailable
