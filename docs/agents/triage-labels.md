# Triage labels

| Role | Label string |
|------|----------------|
| needs-triage | `needs-triage` |
| needs-info | `needs-info` |
| ready-for-agent | `ready-for-agent` |
| ready-for-human | `ready-for-human` |
| wontfix | `wontfix` |

Create on GitHub once: `gh label create needs-triage` (etc.) if missing.
