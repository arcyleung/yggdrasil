# feat/ui Implementation Plan

> **For agentic workers:** Worktree `.worktrees/feat-ui` on branch `feat/ui`. Prefer depending on multi-tenant modules; if missing, implement thin stubs matching control-plane spec §6 API.

**Goal:** FastAPI control plane UI + skill/MCP download + Tailscale Funnel runbook; call auth/token services for lab + demo.

**Architecture:** Spec §3, §6.

**Tech Stack:** FastAPI, Jinja2, python-multipart, itsdangerous sessions; mount or proxy MCP later.

## Global Constraints

- Never render full `sk-` keys in HTML after exchange (show owner + token once).
- `YGG_PUBLIC_BASE_URL` used in skill/mcp templates.
- Lab vs Demo clearly separated in UX copy.
- Work only on `feat/ui` branch in worktree.

## Tasks

### Task UI1: FastAPI app skeleton

**Files:** `src/yggdrasil/web/app.py`, `web/templates/base.html`, `web/static/style.css`, entrypoint `python -m yggdrasil.web` or uvicorn target.

- `/`, `/healthz`

### Task UI2: Lab login + exchange

- Forms + `POST /api/v1/tokens/exchange` (JSON and form).
- Session cookie stores token_id / display owner (not raw key).
- Integrate `AuthService` from multi-tenant if importable; else local stub that reads `user_mapping.yaml` the same way.

### Task UI3: Skill + mcp.json generators

- Templates `templates/skill_lab.md.j2`, `mcp_config.json.j2`
- Routes `/lab/skill.md`, `/lab/mcp.json` require session or `?token=`

### Task UI4: Demo flows

- `/demo`, demo skill, demo token display
- Copy explains isolated demo org

### Task UI5: Funnel / DDNS docs

- `docs/superpowers/runbooks/tailscale-funnel.md`
- README section link

### Task UI6: Tests

- `tests/web/test_app.py` with TestClient — exchange unknown key 401; healthz 200.

**Done when:** UI serves locally; tests pass; skill download contains public URL + bearer placeholder.
