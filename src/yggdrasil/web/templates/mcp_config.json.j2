{
  "mcpServers": {
    "yggdrasil": {
      "url": "{{ mcp_url }}",
      "headers": {
        "Authorization": "Bearer {{ bearer_token }}"
      }
    }
  },
  "_yggdrasil": {
    "public_base_url": "{{ public_base_url }}",
    "owner": "{{ owner }}",
    "tenant_id": "{{ tenant_id }}",
    "transport": "streamable-http",
    "note": "codex mcp add is CLIENT config pointing at this server URL. Bearer must be ygg_… from lab login (not sk-…)."
  }
}
